# 第一版验证记录

## v0.4（2026-09-25）

- 92 项 Node 测试全部通过，TypeScript 检查与生产构建通过。新增覆盖练习事件输入、同步去重/回放、错题答对后消除与历史保留、80 种两小时计划组合、36 道题和语法编号、4 段听力文本对应、表达匹配边界、订正优先级、同步超时与取消。
- 实际 useStudy 模拟 2,000 条额外历史事件及 50 次空闲同步，学习模型引用及重建次数不变；后台停止轮询、跨标签并发写入回归通过。这不是整机内存压测，原内存暴涨原因仍未完整复现。
- 本地同步与 TED API 集成通过，包含 practice 答案上传、第二客户端拉取及重复提交只记一次；私有内容鉴权、音频 Range 和循环标记回归通过。
- 104/104 语法 MP3、4/4 听力 MP3 的解码、非静音、时长、字幕、签名与教材/题库文本对应通过，0 失败；未试听。便携生成脚本另在独立夹具完成 10 项离线回归；旧生成命令增加扩展清单保护。
- 订正构建脚本实际复跑：4 篇、15 处、16 段、1,046 tokens 回拼一致，原始4文件哈希不变；两套私有导入（本地/线上）均4成功、0失败。原文件与公开教材副本未改写。
- 36 条 TED 规则在原始 300 篇语料按两层匹配后命中278篇、1,183处；这是候选识别覆盖率，不是正确率。语言复核范围为首三篇与第六篇指定词注，未逐字审校其余篇目。
- 浏览器本地模拟账号实际操作：答错→解析→错题回练→答对→待错题0且历史2次；20分钟听力组与隐藏原文；TED订正展开、词组/语法高亮、接续讲解、表达加入生词本和复习、待上传归零。390 px 与820 px无水平溢出，平板对话框边界处于视口内并可滚动。
- 未实测：实体iPhone/iPad、真实账号跨实体设备同步、锁屏后台循环、长时间整机内存变化。题库为原创N3衔接N2练习，非完整模拟卷，未由独立教师逐题审核；全部语法/音频和300篇精校仍待后续批次。

音频详情：`docs-voice-pack-verification.md`、`docs-listening-audio-verification.md`。版本历史验收如下。

验证日期：2026-09-24。Windows / Node.js 24.19.0。

- 原教材 JSON、PDF 与原工作目录 SHA-256 一致；原编号和字段保留。
- 10 项自动化测试通过：完整语料、FSRS 并发评分、重试幂等、笔记冲突、UTF-8 分批、状态删除、分页、输入校验和东京日期。
- TypeScript 类型检查与生产构建通过。
- 本地 API 集成检查通过：匿名访问拒绝、伪造认证头拒绝、跨域写入拒绝、账号不匹配拒绝、无效输入、重复提交、第二客户端拉取、增量游标、取消收藏、禁止缓存响应。
- 在浏览器操作验证本地登录、查看语法、添加生词和复习卡、翻面和评分；390 px / 1440 px 视口无横向溢出。
- WebMCP 搜索工具已验证正常查询与无效参数拒绝。
- 三条 MP3 成功生成并通过解码、时长与字幕对应检查，未试听，详见音频报告。

尚未实测：实体 iPhone / iPad 的安装、设备声音、真实账号在两台设备间的同步及断网重启。浏览器尺寸检查不等于实体设备验收。
自动化测试不代表教材语言、日语发音和 JLPT 考试范围已经审校。

## 登录加载修复

- 新增 5 项回归检查，总计 15 项通过：存储配额不足时保留成功登录；退出身份不被旧缓存覆盖；断网恢复缓存；损坏缓存容错；请求超时退出等待。
- 为教材、登录状态请求与 IndexedDB 操作设置截止时间，提供重新加载入口。
- 将离线身份缓存操作与在线认证结果分开；存储被禁用时仍执行服务器退出导航。
- 本次故障观察停在 OpenAI 官方登录页，用户原来使用 Google 登录。以上应用内修复不代表该第三方认证故障已复现或解决。

## v0.2 音色与例句关联

- 24/24 个 MP3 样例成功，失败 0：N5-001～003、N4-001～003，每条两句、Nanami / Keita 两声。解码、时长、非静音、字幕和教材对应通过，详情见 `docs-voice-samples-verification.md`。未试听，不据此确认发音正确。
- 全部 33 项单元测试通过。新增覆盖文件优先/假名及缺片回退、文本变化拒用旧录音、暂停与快速切换、迟到错误、加载超时、音色设置校验和生词关联。
- TypeScript、生产构建、本地同步 API 集成检查通过。
- Chrome 本地实际点击验证：两种音色试听、暂停/续播、切换、保存音色，语法库调用所选 MP3，生词「食べる」显示 N5-002 第二句。此处验证播放行为，未进行听觉与发音审校。
- 390 px 手机宽度下试听弹窗无横向溢出，桌面界面检查通过。实体 iPhone / iPad 未测试；特别是先用浏览器朗读、连续播放后首次进入 MP3 的混合流程仍需设备验证，受阻时提示重新点击例句。
- 保留所有教材原文与词条 ID。2,496 个词记录中 2,366 个获得来源例句，130 个明确显示待确认；132 个词对应两句。该计数不是教材语义或发音审校结论。
- 已核对「つい：不由得」不误配“刚才”用法、「ございます：有」不误配系词用法；未确定的被动/使役/复合形式不回退到任意首句。
- 新旧音频播放互斥；下载整轨与逐句文件继续保留。音频尚未加入离线缓存包，不自动合成全书。

## v0.3 TED 精读与精听

- 300 篇（594 页）完成本机提取、结构/页码/源路径校验。生成 1,592 段，949 对按版面配对，643 段单侧保留。299 篇仍为 OCR 待校对，不是人工审校稿。
- 300 篇分词与查词数据逐字回拼、字典引用、原字段和来源校验全部通过；12 项 Python 词典测试通过。词义覆盖约80.95%，覆盖不是准确率；未知项保持明确空缺。
- 300 条音频全段解码成功（285个唯一文件），失败0；3个异常格式生成兼容 MP3。未试听，不能确认音频和 PDF 内容完全对应。
- 私有云端导入完成：300篇JSON、300份PDF、300条音频；逐文件SHA-256/大小核验及目录D1核验通过，缺失0。重复原件保留编号并提示。
- 全部45项Node测试通过；新增循环状态、暂停/恢复、留白拖动、Range解析、同步重放/删除、长词卡拒绝、表层读音与原形不混挂。播放器状态测试使用实际组件与模拟时钟/媒体，不替代设备播放验收。
- TypeScript与生产构建通过。既有同步API及TED集成检查通过：匿名/伪造身份拒绝、跨域及账号限制、重复事件幂等、多客户端拉取、区域保存/删除、字节Range、私有缓存头、导入密钥拒绝。
- 构建后的本地Worker验证206精确两字节与Content-Range、HEAD长度及匿名401。GET在本地传输层使用chunked；实体Safari未测试，未据此宣称iOS验收通过。
- Chrome实际操作验证查词、加入生词本/复习、两组不同颜色区域、点击区域循环、全文从头播放、暂停及再次打开保留标记/位置；真实音频播放行为已观察，未进行听觉评审。
- 390px手机宽度词卡可滚动且无横向溢出；820px平板宽度与桌面检查通过。未使用实体iPhone/iPad验证安装、触屏、锁屏/后台播放或跨设备真实账号同步。
- 保留原622条语法、1,244个例句、24个音色样例和旧阅读器。TED原件与提取文稿未进入开源仓库；临时导入密钥在资料导入后移除，最终部署关闭导入入口。
