# 内存占用修复与验证（2026-09-25）

用户报告正式首页及本地预览有明显内存增长。本次短时检查未重现数 GB 的持续增长，因此尚不能确认该现象的完整根因。

## 已修复

- IndexedDB 返回内容相同的克隆对象时，复用已有 React 缓存和 FSRS 模型；无变化的事务不再重写完整日志。仍在最新事务中合并，保留同步途中其他标签新增的记录。
- 复用东京时区的日期格式化器，按复习记录变化一次计算每日统计；教材生词不再随着每次同步重新解析。
- 后台标签页暂停定时同步和首页时钟刷新，回到前台立即更新。主动保存后的同步仍保留。
- TED 退出时使用捕获的音频节点停止播放、移除音源并释放解码资源；即使 React 已清空 ref 也会清理。切换文章、退出页面会取消未完成的请求。

没有删除学习记录、修改教材或批量下载 TED。首页不请求 TED 原文和音频；TED 按选中的单篇加载，私人 API 不进入 Service Worker 缓存。

## 验证

- 66 项 Node 回归测试通过；TypeScript 检查及生产构建通过。
- 原教材结构检查通过（622 条、1,244 例句）；全书 dry-run 通过，仅生成计划，未调用语音服务或合成音频。
- 实际 hook 测试覆盖空同步、TED 对象克隆、新远端事件、跨标签 pending、请求途中并发写入、后台暂停与前台恢复。恢复旧的缓存更新方式后，空同步和重复广播测试如预期失败。
- 播放器测试覆盖 ref 已清空的卸载、跟读留白期间退出、Strict Mode 重放；旧实现无法通过新增资源释放测试。
- 本地同步与 TED API 集成测试通过，包含身份隔离、重试幂等、增量游标、音频 Range 请求和循环标记同步。
- Chrome 手动自动化检查：正式首页可正常同步；修复后的本地首页和 TED 可打开，循环播放实际推进；三次往返首页与 TED，首页音频元素数均为 0，两个已有循环区域保留。
- 本次进程抽样未见数 GB 的持续增长。进程内存并不等于单个标签页内存，且开发服务器自身会占用额外内存；这些观察不构成长时间泄漏已经完全消除的证明。

若再次出现明显增长，应补充当时的进程名称、内存数值、打开的网址及操作步骤，以区分网页、本地开发服务、扩展和其他应用。本次未清除用户网站数据，也未关闭其他应用。
